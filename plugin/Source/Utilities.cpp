#include "Utilities.h"

/**
 * Utilities.cpp
 * 
 * Most utility functions are inline in the header for performance,
 * but any that are too complex or need to avoid header bloat go here.
 */

// Currently all utilities are inline in the header.
// This file exists for future expansion and to satisfy the build system.
